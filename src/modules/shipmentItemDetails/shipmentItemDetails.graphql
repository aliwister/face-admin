query shipmentItemDetails($id: ID) {
  shipmentItemDetails(id:$id) {
        id
        sequence
        quantity
        description
        shipmentId
        productId
        image
        po
  }
}
